建立本地仓库
1. 初始化
git init

2. 克隆服务器端的数据（数据仓库）
git clone git@github.com:你的GitHub用户名/你要克隆的仓库名.git

3.添加文件，相当于是将所有的文件，委托给git仓库进行管理
git add ./*

4.将文件提交到本地的git仓库（m的参数必须要添加）
git commit -m "备注信息"

5.将本地代码同步到服务器上
git push origin master

